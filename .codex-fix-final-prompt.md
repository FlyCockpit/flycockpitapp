You are the fresh implementer for one root-cause fix pass only in `/home/hermes/projects/flycockpit/0010`, branch `green4-e2e-rootfix`, current head `d8cf25fa6`. Do not invoke another harness, re-enter review-loop, or wait on stdin. Implement, validate, update the existing audit, and commit when green; do not push.

Read `AGENTS.md`, `audits/green4-e2e-rootfix-pass2.md`, `/tmp/flycockpit-review-final.txt`, and `/tmp/flycockpit-critic-final.txt` in full. The critic’s evidence controls: E2E-DURABLE-POLL remains open because three client-visible daemon-worker `mark_interrupt_interrupted` commit sites lack the exact `InterruptInterrupted` completion event: `finish_parked_replay_completion` around run.rs:4261 and malformed claimed parked-row branches around run.rs:12508 and 12525. Engine initialization writers before `emit_raised` are outside this client-visible completion obligation. The hermetic live-hello readiness loop is not reopened.

Root-cause requirement: establish one production post-commit typed completion funnel for every client-visible daemon-worker transition to `interrupted`, including these three sites and the existing startup reconciliation site. Preserve exact session_id/interrupt_id identity, emit only after a successful state-changing commit, and do not emit for no-op/failed commits. Do not add test-only product hooks. Add focused defect-sensitive regressions for parked replay failure and both malformed claimed parked-row variants (group structurally only if the test truly exercises both). Enumerate all production callers of `mark_interrupt_interrupted` and justify inclusion/exclusion in the audit.

Hard constraints: no retries, longer timeouts, sleeps, poll loops with budgets, fixed waits for real work, serial mutexes, #[ignore], looser/deleted assertions, or cfg(test)/env hooks in product code. Never touch real Cockpit config/data/cache dirs. Before every Cargo invocation run exactly `git ls-files -z | xargs -0 touch`. One Cargo invocation at a time, `CARGO_TARGET_DIR=target`, `-j 3`; targeted tests only, never full workspace suite. Do not install dependencies. Preserve unrelated changes. Use apply_patch for manual file edits. Run `cargo fmt` after edits.

Validation required for the final revision:
1. Narrow focused production regressions covering the new funnel and all new tests.
2. The complete `daemon::server::` module using the command/filter recorded in the audit.
3. The exact user-requested 24-name selection in one tmpfs loaded run, `NEXTEST_TEST_THREADS=4`, using the filter recorded in the audit; report every exact test status.
4. Relevant protocol/consumer checks only if touched beyond existing event routing.
5. Final formatting and diff integrity.

Update `audits/green4-e2e-rootfix-pass2.md` incrementally with the new class sweep, coverage table, commands/run IDs/results/gaps, final per-test results, reviewer/critic evidence and closure status. Commit the green family (no amend, no push), excluding all `.codex-*` prompts. Return commit hash, precise changed mechanisms, enumeration proof, checks/results, and any gap.

Implementer brief (apply verbatim):
1. For each addressed finding, provide `Class sweep: scope searched; affected sites; enforcement point; verification; remaining exceptions`. Under `root_cause`, fix the full class within the goal's scope. If the instance is the full class, justify that bound. Checks or warnings close a finding only if they prevent the prohibited behavior; otherwise the unmet requirement stays open unless explicitly deferred.
2. Maintain a compact coverage table for each changed invariant: `site / operation | holds, violates, or unknown | evidence`. Include consumers and siblings, plus entry, exit, bypass, failure, timeout, shutdown, and re-entry paths where relevant. Give the search scope, queries and results, or structural bound establishing completeness. Group sites only when the same proof covers them; update the artifact incrementally.
3. For shared-state changes, document ownership and lock/permit order; a terminal state is not a lock. For crash/restart-sensitive changes, persist required state and show durable intent before recoverable external effects. Do not add mechanism to compensate for an incomplete inventory.
4. Run assigned validation commands, honoring `test lifecycle` and `skip`; report commands, results, and gaps. Coordinate resource-heavy commands with the coordinator. First-pass tests cover the empty/default case and primary action; regression checks must exercise the defect.
