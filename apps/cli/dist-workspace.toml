[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The release workflow is hand-rolled at the monorepo root (it drives dist with
# --allow-dirty); don't require the dist-generated workflow file to exist.
allow-dirty = ["ci"]
# The installers to generate for each app
installers = ["shell", "powershell", "homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Generated release assets are copied into archives/installers alongside the binary.
# cargo-dist 0.32 `include` only copies pre-existing files, so the release workflow
# runs `scripts/generate-release-assets.sh` before `dist build`.
include = ["target/dist/completions", "target/dist/man", "scripts/install-shell-assets.sh"]
# Where to host releases
hosting = "github"
# Whether to install an updater program
install-updater = false
# Publish the Homebrew formula as `cockpit` (the binary name) rather than the
# `cockpit-cli` package name, so the install line is
# `brew install flycockpit/tap/cockpit`.
formula = "cockpit"
# The GitHub repo that hosts your Homebrew tap. dist generates cockpit.rb as a
# release artifact; the root Release workflow then commits that file into this
# tap with HOMEBREW_TAP_TOKEN.
tap = "FlyCockpit/homebrew-tap"
# Keep this aligned with the root Release workflow's publish-homebrew-formula
# job. If the workflow is regenerated from cargo-dist, review the generated
# Homebrew publish job before replacing the hand-rolled monorepo workflow.
publish-jobs = ["homebrew"]
# The root manual Release workflow consumes this config from the monorepo root.
# Pull-request cargo-dist runs are plan-only when generated locally.
pr-run-mode = "plan"
# Release requests must pass the root release-preflight workflow before artifacts build.
plan-jobs = ["./release-preflight"]

[dist.github-action-commits]
"actions/checkout" = "df4cb1c069e1874edd31b4311f1884172cec0e10"
"actions/upload-artifact" = "043fb46d1a93c77aae656e7c1c64a875d1fc6a0a"
"actions/download-artifact" = "3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c"
