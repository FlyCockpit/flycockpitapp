# State-machine families are the authoritative operational contract shared by
# related tables. Table entries below reference one family instead of copying
# vague recovery prose. Terminal-state spellings are intentionally explicit so
# Rust and SQL transition reviews have one inventory to compare.
[state_machine_family]
"agent_installation" = { rust_owner = "cockpit-db::db::agent_installations and cockpit-core::daemon::agent_installation", recovery_entrypoint = "cockpit-core::daemon::agent_installation::recover_pending_operations before socket publication", terminal_states = "prepared, committed, reconciled, rejected, expired", retention_rule = "retain active bindings and referenced snapshots; purge terminal operation evidence only after file publication settles", authoritative_invariant = "installation revision, definition digest, binding revision, and session snapshot advance atomically or recovery resumes from the durable journal" }
"approval_decision" = { rust_owner = "cockpit-db::db::agent_tree_decisions and cockpit-db::db::needs_attention", recovery_entrypoint = "daemon session rehydration reconstructs unresolved decisions and attention rows before accepting turns", terminal_states = "approved, rejected, cancelled, expired, resolved", retention_rule = "retain terminal receipts with their session audit window", authoritative_invariant = "one terminal receipt settles a request exactly once and belongs to the same session/agent scope" }
"client_submission" = { rust_owner = "cockpit-db::db::sessions and cockpit-core::daemon::session_worker", recovery_entrypoint = "session worker receipt reconciliation during attach/resume", terminal_states = "committed, rejected, cancelled, outcome_unknown", retention_rule = "retain terminal receipts for the session audit window and idempotency horizon", authoritative_invariant = "one client submission identity maps to one canonical request digest and terminal outcome" }
"connector_sync" = { rust_owner = "cockpit-db::db::connector and cockpit-db::db::org_sync", recovery_entrypoint = "remote-profile connector and org-sync boot recovery", terminal_states = "idle, synced, failed, disabled", retention_rule = "remote profile only; retain current cursor plus bounded terminal audit evidence", authoritative_invariant = "remote cursors advance only with the corresponding durable application receipt" }
"external_journal" = { rust_owner = "cockpit-db::db::external_journal", recovery_entrypoint = "cockpit-core::external_journal boot reconciliation before external-effect dispatch", terminal_states = "succeeded, failed, cancelled, completed_after_cancel, outcome_unknown", retention_rule = "retain terminal evidence and tombstones through the external-effect idempotency window", authoritative_invariant = "publication intent commits before an external effect and terminal evidence closes every claimed generation" }
"goal_control" = { rust_owner = "cockpit-db::db::session_goals and cockpit-core::goal", recovery_entrypoint = "session goal-control rehydration on attach/resume", terminal_states = "completed, blocked, cancelled", retention_rule = "retain terminal goal history with the owning session", authoritative_invariant = "only the active goal generation may transition and every terminal transition has one durable receipt" }
"image_generation" = { rust_owner = "cockpit-db::db::image_generation and cockpit-core::daemon::image_generation_worker", recovery_entrypoint = "extended-profile image_generation_worker startup reconciliation", terminal_states = "succeeded, failed, cancelled, expired, cleanup_complete, outcome_unknown", retention_rule = "extended profile only; retain terminal spend/publication evidence until artifacts and external journals settle", authoritative_invariant = "job, slot, attempt, cancellation, publication, artifact, and external-journal generations transition through SQL-registered edges" }
"image_spend" = { rust_owner = "cockpit-db::db::image_spend", recovery_entrypoint = "extended-profile image worker spend-reservation reconciliation", terminal_states = "committed, released, charged, rejected, expired", retention_rule = "extended profile only; retain immutable cost events and terminal reservations for the configured accounting window", authoritative_invariant = "reserved and charged micro-units never exceed the versioned policy without a recorded debt resolution" }
"inference" = { rust_owner = "cockpit-db::db::inference_calls and cockpit-core::engine", recovery_entrypoint = "session worker rehydrates outstanding inference request receipts", terminal_states = "succeeded, failed, cancelled, outcome_unknown", retention_rule = "retain terminal inference attribution with the session usage window", authoritative_invariant = "one inference request identity binds one provider/model snapshot and terminal usage outcome" }
"installation_operation" = { rust_owner = "cockpit-db::db::installation_operations and cockpit-core::daemon::agent_installation", recovery_entrypoint = "agent-installation operation and file-journal reconciliation before config publication", terminal_states = "terminal, completed, expired", retention_rule = "retain terminal operation receipts through the retry horizon; referenced installation snapshots remain", authoritative_invariant = "staged file metadata, database revision, rename checkpoint, and terminal receipt settle in order" }
"local_operation" = { rust_owner = "cockpit-db::db::local_operation_receipts", recovery_entrypoint = "owning daemon mutation service reconciles its journal before accepting a retry", terminal_states = "committed, rejected, outcome_unknown", retention_rule = "retain through the local idempotency horizon and while a mutation journal references the operation", authoritative_invariant = "principal, request identity, fencing generation, and terminal response are immutable for one client operation id" }
"config_mutation" = { rust_owner = "cockpit-db::db local operation journals and cockpit-core::daemon::fs_api", recovery_entrypoint = "daemon config/agent/MCP mutation-journal reconciliation before socket publication", terminal_states = "committed, reconciled, rejected, outcome_unknown", retention_rule = "delete only after exact terminal settlement; retain divergent hash-only markers for inspection", authoritative_invariant = "compare-and-swap revision and intended document identity fence every atomic file publication" }
"lock" = { rust_owner = "cockpit-db::db::locks and cockpit-core::lock_manager", recovery_entrypoint = "daemon lock-manager reconstruction before session work resumes", terminal_states = "released, expired, revoked", retention_rule = "remove settled lock mirrors after recovery evidence is no longer needed", authoritative_invariant = "lock generation and session ownership fence every acquire, renew, and release" }
"media" = { rust_owner = "cockpit-db::db::media_attachments and cockpit-core::media_storage", recovery_entrypoint = "daemon media storage and reservation reconciliation before media workers start", terminal_states = "ready, failed, cancelled, released, deleted, outcome_unknown", retention_rule = "retain admitted media with live references; purge terminal processing and cleanup evidence by media retention policy", authoritative_invariant = "reservation, publication, component lease, processing, and deletion generations reconcile without orphaning bytes" }
"protected_leak" = { rust_owner = "cockpit-db::db::protected_leak_records and cockpit-core::leaks", recovery_entrypoint = "protected-history reference reconciliation during daemon database boot", terminal_states = "contained, rotated, superseded, deleted", retention_rule = "forget ciphertext only after references settle; retain safe report metadata for the security audit window", authoritative_invariant = "plaintext never enters the report row and retirement atomically zeroes protected material after reference count reaches zero" }
"remote_attachment" = { rust_owner = "cockpit-db::db::remote_attachment_operations and cockpit-core remote attachment services", recovery_entrypoint = "remote-profile attachment, audit-upload, custody, and rename-artifact boot recovery", terminal_states = "committed, rejected, cancelled, expired, outcome_unknown", retention_rule = "remote profile only; retain terminal custody/effect evidence through the remote idempotency window", authoritative_invariant = "authenticated device generation and logical attachment identity fence every remote mutation" }
"scheduled_job" = { rust_owner = "cockpit-db::db::scheduler and cockpit-core::daemon::scheduler", recovery_entrypoint = "extended-profile DaemonScheduler::recompute_after_start", terminal_states = "disabled, deleted, succeeded, failed", retention_rule = "extended profile only; retain the current job and bounded last-result evidence until explicit deletion", authoritative_invariant = "one scheduler owns due-time computation and persists each run result before advancing next_run_at" }
"sealed" = { rust_owner = "cockpit-db::db::sealed_values, sealed_scope, and sealed_actions", recovery_entrypoint = "sealed value/action saga reconciliation before owner reveal or action execution", terminal_states = "committed, retired, rejected, expired, outcome_unknown", retention_rule = "retain safe audit/tombstone evidence; destroy protected value material only after references and sagas settle", authoritative_invariant = "scope generation, version, capability binding, and publish-before-reveal audit fence every protected operation" }
"secret_vault" = { rust_owner = "cockpit-db::db::secret_vault and cockpit-core::secure_key", recovery_entrypoint = "secret vault authority and wrap-key saga reconciliation during daemon vault open", terminal_states = "active, retired, completed, rejected, outcome_unknown", retention_rule = "retain active wrapped keys and ownership claims; purge terminal saga evidence after every consumer migrates", authoritative_invariant = "KEK plaintext never enters SQLite and one authority generation owns every wrapped DEK transition" }
"secure_key" = { rust_owner = "cockpit-db::db::secure_key", recovery_entrypoint = "secure-key namespace/version saga reconciliation during vault boot", terminal_states = "active, retired, completed, rejected", retention_rule = "retain versions while consumers reference them and terminal saga evidence through key-rotation settlement", authoritative_invariant = "namespace generation and consumer references prevent retirement of an in-use key version" }
"session" = { rust_owner = "cockpit-db::db::sessions, session_log, text_artifacts, and run_invocations", recovery_entrypoint = "daemon session registry rehydration and session-worker attach/resume", terminal_states = "completed, failed, cancelled, archived, deleted, outcome_unknown", retention_rule = "retain session history under configured transcript/payload windows; pins and preservation holds exempt rows", authoritative_invariant = "session sequence, event identity, model envelope, artifact quota, and run invocation remain scoped to one session generation" }
"task" = { rust_owner = "cockpit-db::db::task_delegations, task_delegation_payloads, and workspace_lease_artifacts", recovery_entrypoint = "delegation payload prepare/cleanup and task artifact reconciliation during daemon boot", terminal_states = "completed, failed, cancelled, expired, integrated, outcome_unknown", retention_rule = "retain terminal delegation/artifact evidence with the parent session; delete sidecars only through durable cleanup intents", authoritative_invariant = "delegation generation, payload digest, sidecar intent, child identity, and artifact integration receipt settle exactly once" }
"telemetry" = { rust_owner = "cockpit-db::db::tool_calls and cockpit-db::db::usage_events", recovery_entrypoint = "no active recovery; append completion closes each invocation and doctor reports unterminated rows", terminal_states = "succeeded, failed, cancelled", retention_rule = "retain tool and usage attribution under configured payload/telemetry windows", authoritative_invariant = "one call identity has immutable request attribution and at most one terminal completion" }
"verification" = { rust_owner = "cockpit-db::db::verification_ledger and cockpit-core verification services", recovery_entrypoint = "verification operation/projection reconciliation during session resume", terminal_states = "verified, rejected, failed, cancelled, expired, synthesized", retention_rule = "retain terminal verification and synthesis evidence with the owning session audit window", authoritative_invariant = "candidate, dispatch, projection, synthesis, and artifact generations remain bound to one verification operation" }
"workspace_write" = { rust_owner = "cockpit-db::db::workspace_lease_artifacts and cockpit-db::db::write_scope_leases", recovery_entrypoint = "workspace/write-scope lease expiry and transfer reconciliation before mutable tool work resumes", terminal_states = "released, revoked, expired, committed, rejected", retention_rule = "remove settled leases after recovery; retain transfer/permit evidence for the workspace audit window", authoritative_invariant = "lease generation, canonical workspace identity, permit scope, and transfer receipt fence every filesystem mutation" }

# Authoritative per-table launch ownership. Every executed table must have one
# complete entry; the SQLite-backed contract rejects missing fields and stale objects.
[table]
"agent_binding_receipts" = { domain = "agent", rust_owner = "cockpit_db::db::agent_installations table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:agent_installation", retention_policy = "retained through agent installation recovery and active session references; terminal journals purge only after publication settles", state_machine = "rust-and-sql", state_machine_family = "agent_installation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "versioned agent definition and binding history required by the public agent workflow", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_installations table accessors plus 0001_initial.sql" }
"agent_installations" = { domain = "agent", rust_owner = "cockpit_db::db::installation_operations + cockpit_db::db::agent_installations table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::installation_operations + cockpit_db::db::agent_installations table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained through agent installation recovery and active session references; terminal journals purge only after publication settles", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "versioned agent definition and binding history required by the public agent workflow", launch_profile = "local", invariant_owner = "cockpit_db::db::installation_operations + cockpit_db::db::agent_installations table accessors plus 0001_initial.sql" }
"agent_instances" = { domain = "agent", rust_owner = "cockpit_db::db::verification_ledger + cockpit_db::db::agent_tree_decisions table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::verification_ledger + cockpit_db::db::agent_tree_decisions table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger + cockpit_db::db::agent_tree_decisions table accessors plus 0001_initial.sql" }
"agent_model_bindings" = { domain = "agent", rust_owner = "cockpit_db::db::installation_operations + cockpit_db::db::agent_installations table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::installation_operations + cockpit_db::db::agent_installations table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained through agent installation recovery and active session references; terminal journals purge only after publication settles", state_machine = "rust", semantic_class = "junction", identity_policy = "declared composite or stable association identity; relationship scope is part of identity", snapshot_policy = "versioned agent definition and binding history required by the public agent workflow", launch_profile = "local", invariant_owner = "cockpit_db::db::installation_operations + cockpit_db::db::agent_installations table accessors plus 0001_initial.sql" }
"agent_profile_snapshots" = { domain = "agent", rust_owner = "cockpit_db::db::agent_tree_decisions + cockpit_db::db::agent_installations table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::agent_tree_decisions + cockpit_db::db::agent_installations table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained through agent installation recovery and active session references; terminal journals purge only after publication settles", state_machine = "rust", semantic_class = "projection", identity_policy = "source identity plus projection generation or declared projection key", snapshot_policy = "versioned agent definition and binding history required by the public agent workflow", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_tree_decisions + cockpit_db::db::agent_installations table accessors plus 0001_initial.sql" }
"agent_session_preparation_claims" = { domain = "agent", rust_owner = "cockpit_db::db::agent_installations table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:agent_installation", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "agent_installation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_installations table accessors plus 0001_initial.sql" }
"agent_session_preparations" = { domain = "agent", rust_owner = "cockpit_db::db::agent_installations table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:agent_installation", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "agent_installation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_installations table accessors plus 0001_initial.sql" }
"agent_transition_receipts" = { domain = "agent", rust_owner = "cockpit_db::db::agent_tree_decisions table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:agent_installation", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "agent_installation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_tree_decisions table accessors plus 0001_initial.sql" }
"app_flags" = { domain = "app", rust_owner = "cockpit_db::db::remote_attachment_operations + cockpit_db::db::app_flags table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::remote_attachment_operations + cockpit_db::db::app_flags table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::remote_attachment_operations + cockpit_db::db::app_flags table accessors plus 0001_initial.sql" }
"approval_grants" = { domain = "approval", rust_owner = "cockpit_db::db + cockpit_core::session::export + cockpit_core::session::import table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:approval_decision", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "approval_decision", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db + cockpit_core::session::export + cockpit_core::session::import table accessors plus 0001_initial.sql" }
"assistants" = { domain = "assistants", rust_owner = "cockpit_db::db::assistants table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::assistants table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::assistants table accessors plus 0001_initial.sql" }
"client_submission_terminal_receipts" = { domain = "client", rust_owner = "cockpit_db::db::session_log + cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:client_submission", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "client_submission", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::session_log + cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"compaction_handoffs" = { domain = "compaction", rust_owner = "cockpit_db::db::session_search + cockpit_db::db::session_log table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::session_search + cockpit_db::db::session_log table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::session_search + cockpit_db::db::session_log table accessors plus 0001_initial.sql" }
"compaction_shadows" = { domain = "compaction", rust_owner = "cockpit_db::db::shadow_store table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::shadow_store table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::shadow_store table accessors plus 0001_initial.sql" }
"connector_state" = { domain = "connector", rust_owner = "cockpit_db::db::connector table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:connector_sync", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "connector_sync", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_db::db::connector table accessors plus 0001_remote_profile.sql" }
"decision_attention_mutation_guards" = { domain = "decision", rust_owner = "cockpit_db::db::agent_tree_decisions table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::agent_tree_decisions table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_tree_decisions table accessors plus 0001_initial.sql" }
"decision_receipts" = { domain = "decision", rust_owner = "cockpit_db::db::agent_tree_decisions table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:approval_decision", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "approval_decision", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_tree_decisions table accessors plus 0001_initial.sql" }
"decision_requests" = { domain = "decision", rust_owner = "cockpit_db::db::agent_tree_decisions table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:approval_decision", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "approval_decision", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_tree_decisions table accessors plus 0001_initial.sql" }
"execution_containments" = { domain = "execution", rust_owner = "cockpit_db::db::execution_containments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::execution_containments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::execution_containments table accessors plus 0001_initial.sql" }
"external_journal_events" = { domain = "external", rust_owner = "cockpit_db::db::external_journal table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:external_journal", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "external_journal", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::external_journal table accessors plus 0001_initial.sql" }
"external_journal_integrity_faults" = { domain = "external", rust_owner = "cockpit_db::db::external_journal table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::external_journal table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::external_journal table accessors plus 0001_initial.sql" }
"external_journal_operations" = { domain = "external", rust_owner = "cockpit_db::db::image_spend + cockpit_db::db::external_journal + cockpit_db::db::image_generation table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:external_journal", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "external_journal", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::image_spend + cockpit_db::db::external_journal + cockpit_db::db::image_generation table accessors plus 0001_initial.sql" }
"external_journal_queue_entries" = { domain = "external", rust_owner = "cockpit_db::db::external_journal table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::external_journal table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::external_journal table accessors plus 0001_initial.sql" }
"external_journal_session_tombstones" = { domain = "external", rust_owner = "cockpit_db::db::external_journal table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:external_journal", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "external_journal", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::external_journal table accessors plus 0001_initial.sql" }
"external_journal_spool_capsules" = { domain = "external", rust_owner = "cockpit_db::db::external_journal table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::external_journal table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::external_journal table accessors plus 0001_initial.sql" }
"goal_control_jobs" = { domain = "goal", rust_owner = "cockpit_db::db::session_goals table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:goal_control", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "goal_control", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::session_goals table accessors plus 0001_initial.sql" }
"goal_root_turns" = { domain = "goal", rust_owner = "cockpit_db::db::session_goals table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::session_goals table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::session_goals table accessors plus 0001_initial.sql" }
"guidance_contents" = { domain = "guidance", rust_owner = "cockpit_db::db::guidance table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::guidance table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::guidance table accessors plus 0001_initial.sql" }
"image_generation_artifact_authorization_facts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_artifact_cleanup_intents" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_artifact_components" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "junction", identity_policy = "declared composite or stable association identity; relationship scope is part of identity", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_artifact_leases" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_artifact_references" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_artifacts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_artifact_security_recovery_attempts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_artifact_security_recovery_audits" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_artifact_security_recovery_components" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "junction", identity_policy = "declared composite or stable association identity; relationship scope is part of identity", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_artifact_transitions" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_attempt_activation_facts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_attempt_media_snapshots" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "projection", identity_policy = "source identity plus projection generation or declared projection key", snapshot_policy = "rebuildable denormalization; canonical source identity remains authoritative", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_attempts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_attempt_transitions" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_cancellation_facts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_cancelled_result_facts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_cleanup_transitions" = { domain = "image", rust_owner = "cockpit-db::db::image_generation::Db methods", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit-db::db::image_generation::Db methods plus 0001_extended_profile.sql" }
"image_generation_component_release_facts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_component_transitions" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_deadline_expiry_facts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_handoff_evidence" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_jobs" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_job_transitions" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_late_publication_authorization_facts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_late_publication_leases" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_late_publication_transitions" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_plans" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_provider_cancel_claims" = { domain = "image", rust_owner = "cockpit_core::image_generation_job table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_core::image_generation_job table accessors plus 0001_extended_profile.sql" }
"image_generation_provider_cancel_evidence" = { domain = "image", rust_owner = "cockpit_core::image_generation_job table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_core::image_generation_job table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_core::image_generation_job table accessors plus 0001_extended_profile.sql" }
"image_generation_publication_right_facts" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_reconciliation_claim_completions" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_reconciliation_claims" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_reconciliation_evidence" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_response_fetches" = { domain = "image", rust_owner = "cockpit_core::image_generation_job table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_core::image_generation_job table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_core::image_generation_job table accessors plus 0001_extended_profile.sql" }
"image_generation_response_fetch_outcomes" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_response_publication_intents" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_response_reconciliation_claims" = { domain = "image", rust_owner = "cockpit_core::image_generation_job table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_core::image_generation_job table accessors plus 0001_extended_profile.sql" }
"image_generation_response_reconciliations" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_scheduler_claim_consumptions" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_scheduler_claims" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_scheduler_error_counts" = { domain = "image", rust_owner = "cockpit_core::image_generation_job table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_core::image_generation_job table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_core::image_generation_job table accessors plus 0001_extended_profile.sql" }
"image_generation_slots" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_slot_transitions" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_terminal_events" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_generation", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_generation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_generation_user_published_outputs" = { domain = "image", rust_owner = "cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_generation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_spend_attempt_dispatches" = { domain = "image", rust_owner = "cockpit_db::db::image_spend table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_spend", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_spend", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_spend table accessors plus 0001_extended_profile.sql" }
"image_spend_attempts" = { domain = "image", rust_owner = "cockpit_db::db::image_spend table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_spend", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_spend", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_spend table accessors plus 0001_extended_profile.sql" }
"image_spend_cost_events" = { domain = "image", rust_owner = "cockpit_db::db::image_spend table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_spend", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_spend", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_spend table accessors plus 0001_extended_profile.sql" }
"image_spend_debt_resolutions" = { domain = "image", rust_owner = "cockpit_db::db::image_spend table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_spend table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_spend table accessors plus 0001_extended_profile.sql" }
"image_spend_epoch_heads" = { domain = "image", rust_owner = "cockpit_db::db::image_spend table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_spend table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_spend table accessors plus 0001_extended_profile.sql" }
"image_spend_policy_versions" = { domain = "image", rust_owner = "cockpit_db::db::image_spend table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_spend", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_spend", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_spend table accessors plus 0001_extended_profile.sql" }
"image_spend_reservations" = { domain = "image", rust_owner = "cockpit_db::db::image_spend + cockpit_db::db::image_generation table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:image_spend", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "image_spend", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_spend + cockpit_db::db::image_generation table accessors plus 0001_extended_profile.sql" }
"image_spend_scope_usage" = { domain = "image", rust_owner = "cockpit_db::db::image_spend table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::image_spend table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::image_spend table accessors plus 0001_extended_profile.sql" }
"inference_calls" = { domain = "inference", rust_owner = "cockpit_db::db::sessions + cockpit_db::db::archive_import + cockpit_db::db::stats table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::sessions + cockpit_db::db::archive_import + cockpit_db::db::stats table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sessions + cockpit_db::db::archive_import + cockpit_db::db::stats table accessors plus 0001_initial.sql" }
"inference_requests" = { domain = "inference", rust_owner = "cockpit_db::db::tandem + cockpit_db::db::session_log + cockpit_db::db::inference_calls table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:inference", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "inference", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::tandem + cockpit_db::db::session_log + cockpit_db::db::inference_calls table accessors plus 0001_initial.sql" }
"installation_continuations" = { domain = "installation", rust_owner = "cockpit_db::db::installation_operations table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:installation_operation", retention_policy = "retained through agent installation recovery and active session references; terminal journals purge only after publication settles", state_machine = "rust-and-sql", state_machine_family = "installation_operation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "versioned agent definition and binding history required by the public agent workflow", launch_profile = "local", invariant_owner = "cockpit_db::db::installation_operations table accessors plus 0001_initial.sql" }
"installation_identity" = { domain = "installation", rust_owner = "cockpit_db::db::installation_identity table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::installation_identity table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained through agent installation recovery and active session references; terminal journals purge only after publication settles", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "versioned agent definition and binding history required by the public agent workflow", launch_profile = "local", invariant_owner = "cockpit_db::db::installation_identity table accessors plus 0001_initial.sql" }
"installation_journals" = { domain = "installation", rust_owner = "cockpit_db::db::installation_operations table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::installation_operations table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained through agent installation recovery and active session references; terminal journals purge only after publication settles", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "versioned agent definition and binding history required by the public agent workflow", launch_profile = "local", invariant_owner = "cockpit_db::db::installation_operations table accessors plus 0001_initial.sql" }
"installation_observations" = { domain = "installation", rust_owner = "cockpit_db::db::agent_installations table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::agent_installations table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained through agent installation recovery and active session references; terminal journals purge only after publication settles", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "versioned agent definition and binding history required by the public agent workflow", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_installations table accessors plus 0001_initial.sql" }
"installation_operations" = { domain = "installation", rust_owner = "cockpit_db::db::installation_operations table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:installation_operation", retention_policy = "retained through agent installation recovery and active session references; terminal journals purge only after publication settles", state_machine = "rust-and-sql", state_machine_family = "installation_operation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "versioned agent definition and binding history required by the public agent workflow", launch_profile = "local", invariant_owner = "cockpit_db::db::installation_operations table accessors plus 0001_initial.sql" }
"intel_callsites" = { domain = "intel", rust_owner = "cockpit_core::intel::callgraph + cockpit_core::intel table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::intel::callgraph + cockpit_core::intel table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::intel::callgraph + cockpit_core::intel table accessors plus 0001_initial.sql" }
"intel_centrality" = { domain = "intel", rust_owner = "cockpit_core::intel::callgraph table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::intel::callgraph table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::intel::callgraph table accessors plus 0001_initial.sql" }
"intel_deps" = { domain = "intel", rust_owner = "cockpit_core::intel + cockpit_core::intel::lang table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::intel + cockpit_core::intel::lang table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::intel + cockpit_core::intel::lang table accessors plus 0001_initial.sql" }
"intel_files" = { domain = "intel", rust_owner = "cockpit_core::intel + cockpit_core::intel::lang table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::intel + cockpit_core::intel::lang table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::intel + cockpit_core::intel::lang table accessors plus 0001_initial.sql" }
"intel_identifiers" = { domain = "intel", rust_owner = "cockpit_core::intel table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::intel table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::intel table accessors plus 0001_initial.sql" }
"intel_imports" = { domain = "intel", rust_owner = "cockpit_core::intel + cockpit_core::intel::lang table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::intel + cockpit_core::intel::lang table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::intel + cockpit_core::intel::lang table accessors plus 0001_initial.sql" }
"intel_meta" = { domain = "intel", rust_owner = "cockpit_core::intel + cockpit_core::knowledge table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::intel + cockpit_core::knowledge table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained for the lifetime of the owning schema profile", state_machine = "rust", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::intel + cockpit_core::knowledge table accessors plus 0001_initial.sql" }
"intel_symbols" = { domain = "intel", rust_owner = "cockpit_core::intel::callgraph + cockpit_core::intel table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::intel::callgraph + cockpit_core::intel table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::intel::callgraph + cockpit_core::intel table accessors plus 0001_initial.sql" }
"local_media_operation_audit" = { domain = "local", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"local_media_operations" = { domain = "local", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:local_operation", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "local_operation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"lock_reads" = { domain = "lock", rust_owner = "cockpit_db::db::locks table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::locks table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::locks table accessors plus 0001_initial.sql" }
"lock_state" = { domain = "lock", rust_owner = "cockpit_db::db::locks table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:lock", retention_policy = "retained for the lifetime of the owning schema profile", state_machine = "rust-and-sql", state_machine_family = "lock", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::locks table accessors plus 0001_initial.sql" }
"loop_guard_rules" = { domain = "loop", rust_owner = "cockpit_core::session::export + cockpit_core::approval::store table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::session::export + cockpit_core::approval::store table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::session::export + cockpit_core::approval::store table accessors plus 0001_initial.sql" }
"mcp_config_journals" = { domain = "mcp", rust_owner = "cockpit_db::db::local_operation_receipts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:config_mutation", retention_policy = "retire atomically with the terminal receipt after file/vault convergence", state_machine = "rust-and-sql", state_machine_family = "config_mutation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::local_operation_receipts table accessors plus 0001_initial.sql" }
"media_accounting_blocks" = { domain = "media", rust_owner = "cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_reservation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_accounting_corruption_facts" = { domain = "media", rust_owner = "cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_artifact_facts" = { domain = "media", rust_owner = "cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_attachment_cleanup_intents" = { domain = "media", rust_owner = "cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_attachment_component_leases" = { domain = "media", rust_owner = "cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_attachment_components" = { domain = "media", rust_owner = "cockpit_db::db::sessions + cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::sessions + cockpit_db::db::media_attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "follows the owning entity and declared foreign-key deletion policy", state_machine = "rust", semantic_class = "junction", identity_policy = "declared composite or stable association identity; relationship scope is part of identity", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sessions + cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_attachment_failure_reasons" = { domain = "media", rust_owner = "cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::media_attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_attachment_processing_cleanup_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_attachment_processing_failure_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_attachment_processing_jobs" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_attachment_processing_output_security_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_attachment_processing_publication_intents" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_attachment_processing_security_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_attachment_references" = { domain = "media", rust_owner = "cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::media_attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_attachments" = { domain = "media", rust_owner = "cockpit_db::db::sessions + cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::sessions + cockpit_db::db::media_attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sessions + cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_attachment_transition_evidence" = { domain = "media", rust_owner = "cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::media_attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_attachment_upload_origins" = { domain = "media", rust_owner = "cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::media_attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_av_normalization_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_cleanup_attestations" = { domain = "media", rust_owner = "cockpit_core::media_reservation + cockpit_core::daemon::server::attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_reservation + cockpit_core::daemon::server::attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation + cockpit_core::daemon::server::attachments table accessors plus 0001_initial.sql" }
"media_cleanup_security_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_component_deletion_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_component_deletion_intents" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_component_lease_reconciliation_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_component_security_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_counter_shadow" = { domain = "media", rust_owner = "cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_reservation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "rebuildable projection retained with its authoritative source and replaced only by generation-aware rebuild", state_machine = "rust", semantic_class = "projection", identity_policy = "source identity plus projection generation or declared projection key", snapshot_policy = "rebuildable denormalization; canonical source identity remains authoritative", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_creation_sequence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained for the lifetime of the owning schema profile", state_machine = "rust", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_downstream_ownership" = { domain = "media", rust_owner = "cockpit_core::media_storage + cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage + cockpit_core::media_reservation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage + cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_execution_ready" = { domain = "media", rust_owner = "cockpit_db::db + cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db + cockpit_core::media_reservation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db + cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_image_component_dimensions" = { domain = "media", rust_owner = "cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::media_attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_ingress_admission_receipts" = { domain = "media ingress", rust_owner = "cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::media_attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained with the admitted attachment and removed by its session/attachment cascade", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_ingress_publication_intents" = { domain = "media ingress", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained until terminal publication settlement removes the intent or session deletion cascades it", state_machine = "rust", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_local_path_registration_audit" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_local_path_registration_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_local_path_registration_operations" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_queue_sequence" = { domain = "media", rust_owner = "cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_reservation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained for the lifetime of the owning schema profile", state_machine = "rust", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_repair_attempts" = { domain = "media", rust_owner = "cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_reservation_deltas" = { domain = "media", rust_owner = "cockpit_core::media_reservation + cockpit_core::daemon::server::attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_reservation + cockpit_core::daemon::server::attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation + cockpit_core::daemon::server::attachments table accessors plus 0001_initial.sql" }
"media_reservation_plan_facts" = { domain = "media", rust_owner = "cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_reservations" = { domain = "media", rust_owner = "cockpit_db::db::image_generation table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::image_generation table accessors plus 0001_initial.sql" }
"media_reservation_versions" = { domain = "media", rust_owner = "cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_resource_counters" = { domain = "media", rust_owner = "cockpit_core::media_reservation + cockpit_core::daemon::server::attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_reservation + cockpit_core::daemon::server::attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained for the lifetime of the owning schema profile", state_machine = "rust", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation + cockpit_core::daemon::server::attachments table accessors plus 0001_initial.sql" }
"media_retained_https_audit" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_retained_https_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_retained_https_operations" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_retained_https_orphan_cleanup_evidence" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_retained_https_publication_intents" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_scheduler_cursor" = { domain = "media", rust_owner = "cockpit_core::media_reservation table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_reservation table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained for the lifetime of the owning schema profile", state_machine = "rust", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_reservation table accessors plus 0001_initial.sql" }
"media_security_recovery_operations" = { domain = "media", rust_owner = "cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"media_storage_publication_intents" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_upload_chunks" = { domain = "media", rust_owner = "cockpit_core::media_storage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::media_storage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::media_storage table accessors plus 0001_initial.sql" }
"media_uploads" = { domain = "media", rust_owner = "cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:media", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "media", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"message_attachment_references" = { domain = "message", rust_owner = "cockpit_db::db::message_attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::message_attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::message_attachments table accessors plus 0001_initial.sql" }
"message_operation_receipts" = { domain = "message", rust_owner = "cockpit_db::db::message_attachments + cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:client_submission", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "client_submission", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::message_attachments + cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"local_operation_receipts" = { domain = "local authority settlement", rust_owner = "cockpit_db::db::local_operation_receipts + cockpit_db::db::retention table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:local_operation", retention_policy = "bounded daemon-owned receipt retention", state_machine = "rust-and-sql", state_machine_family = "local_operation", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::local_operation_receipts + cockpit_db::db::retention table accessors plus 0001_initial.sql" }
"agent_editor_leases" = { domain = "agent management", rust_owner = "cockpit_db::db::agent_editor_leases + cockpit_db::db::retention table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:config_mutation", retention_policy = "SQLite retains only phase, keyed projection identities, exact consumed/result config generations, revision evidence, and secret-free terminal receipts; the vault holds the submitted completion payload until exact terminal settlement, then deletes it without decryption in the same SQLite transaction that terminalizes the lease; expired opens and old terminal receipts are daemon-pruned", state_machine = "rust-and-sql", state_machine_family = "config_mutation", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_editor_leases + cockpit_db::db::retention table accessors plus 0001_initial.sql" }
"agent_mutation_journals" = { domain = "agent management", rust_owner = "cockpit_db::db::agent_mutation_journals + cockpit_db::db::local_operation_receipts + cockpit_db::db::retention table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:config_mutation", retention_policy = "retired with exact terminal local-operation settlement; ambiguous divergent projections retain the hash-only marker", state_machine = "rust-and-sql", state_machine_family = "config_mutation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::agent_mutation_journals + cockpit_db::db::local_operation_receipts + cockpit_db::db::retention table accessors plus 0001_initial.sql" }
"assistant_mutation_journals" = { domain = "assistant management", rust_owner = "cockpit_db::db::local_operation_receipts + cockpit_db::db::retention table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:config_mutation", retention_policy = "retired with exact terminal local-operation settlement; ambiguous divergent projections retain the hash-only marker", state_machine = "rust-and-sql", state_machine_family = "config_mutation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::local_operation_receipts + cockpit_db::db::retention table accessors plus 0001_initial.sql" }
"extended_config_patch_journals" = { domain = "typed settings", rust_owner = "cockpit_db::db::local_operation_receipts + cockpit_db::db::retention table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:config_mutation", retention_policy = "deleted atomically with terminal local-operation settlement; ambiguous divergent files retain the hash-only marker", state_machine = "rust-and-sql", state_machine_family = "config_mutation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::local_operation_receipts + cockpit_db::db::retention table accessors plus 0001_initial.sql" }
"image_config_mutation_journals" = { domain = "image generation config", rust_owner = "cockpit_db::db::retention table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:config_mutation", retention_policy = "deleted after exact terminal local-operation settlement; raw secret-bearing registry bytes never enter SQLite and divergent hash-only markers remain inspectable", state_machine = "rust-and-sql", state_machine_family = "config_mutation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::retention table accessors plus 0001_initial.sql" }
"message_queue_items" = { domain = "message", rust_owner = "cockpit_db::db::message_attachments + cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::message_attachments + cockpit_db::db::text_artifacts table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::message_attachments + cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"message_submission_receipts" = { domain = "message", rust_owner = "cockpit_db::db::message_attachments + cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:client_submission", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "client_submission", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::message_attachments + cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"needs_attention" = { domain = "needs", rust_owner = "cockpit_db::db::needs_attention + cockpit_db::db::sessions + cockpit_db::db::agent_tree_decisions table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::needs_attention + cockpit_db::db::sessions + cockpit_db::db::agent_tree_decisions table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::needs_attention + cockpit_db::db::sessions + cockpit_db::db::agent_tree_decisions table accessors plus 0001_initial.sql" }
"packages" = { domain = "packages", rust_owner = "cockpit_db::db::packages + cockpit_db::db::media_attachments table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::packages + cockpit_db::db::media_attachments table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained until explicit package prune or removal; required by the public ask workflow", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative package source registry used by the public ask workflow", launch_profile = "local", invariant_owner = "cockpit_db::db::packages + cockpit_db::db::media_attachments table accessors plus 0001_initial.sql" }
"paused_session_work" = { domain = "paused", rust_owner = "cockpit_db::db::paused_work table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::paused_work table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::paused_work table accessors plus 0001_initial.sql" }
"pins" = { domain = "pins", rust_owner = "cockpit_db::db::external_journal + cockpit_db::db::sessions + cockpit_db::db::locks table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::external_journal + cockpit_db::db::sessions + cockpit_db::db::locks table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::external_journal + cockpit_db::db::sessions + cockpit_db::db::locks table accessors plus 0001_initial.sql" }
"project_notes" = { domain = "project", rust_owner = "cockpit_db::db::project_notes table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::project_notes table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::project_notes table accessors plus 0001_initial.sql" }
"protected_leak_records" = { domain = "protected", rust_owner = "cockpit_db::db::protected_leak_records table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:protected_leak", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "protected_leak", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::protected_leak_records table accessors plus 0001_initial.sql" }
"protected_redaction_artifact_refs" = { domain = "protected", rust_owner = "cockpit_db::db::protected_redaction_history table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::protected_redaction_history table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "follows the owning entity and declared foreign-key deletion policy", state_machine = "rust", semantic_class = "junction", identity_policy = "declared composite or stable association identity; relationship scope is part of identity", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::protected_redaction_history table accessors plus 0001_initial.sql" }
"protected_redaction_history" = { domain = "protected", rust_owner = "cockpit_db::db::protected_redaction_history + cockpit_db::db::protected_leak_records table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::protected_redaction_history + cockpit_db::db::protected_leak_records table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::protected_redaction_history + cockpit_db::db::protected_leak_records table accessors plus 0001_initial.sql" }
"provider_config_journals" = { domain = "provider", rust_owner = "cockpit_db::db::local_operation_receipts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:config_mutation", retention_policy = "retire atomically with the terminal receipt after file/vault convergence", state_machine = "rust-and-sql", state_machine_family = "config_mutation", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::local_operation_receipts table accessors plus 0001_initial.sql" }
"prune_ledger" = { domain = "prune", rust_owner = "cockpit_db::db::prune_ledger table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::prune_ledger table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::prune_ledger table accessors plus 0001_initial.sql" }
"remote_attachment_lifecycle" = { domain = "remote", rust_owner = "cockpit_db::db::remote_attachment_operations table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::remote_attachment_operations table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_db::db::remote_attachment_operations table accessors plus 0001_remote_profile.sql" }
"remote_attachment_operations" = { domain = "remote", rust_owner = "cockpit_db::db::remote_attachment_operations table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:remote_attachment", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "remote_attachment", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_db::db::remote_attachment_operations table accessors plus 0001_remote_profile.sql" }
"remote_attachment_outbox" = { domain = "remote", rust_owner = "cockpit_db::db::remote_attachment_operations table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::remote_attachment_operations table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_db::db::remote_attachment_operations table accessors plus 0001_remote_profile.sql" }
"remote_attachment_outbox_deliveries" = { domain = "remote", rust_owner = "cockpit_db::db::remote_attachment_operations table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::remote_attachment_operations table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_db::db::remote_attachment_operations table accessors plus 0001_remote_profile.sql" }
"remote_attachment_outbox_snapshots" = { domain = "remote", rust_owner = "cockpit_db::db::remote_attachment_operations table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::remote_attachment_operations table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "projection", identity_policy = "source identity plus projection generation or declared projection key", snapshot_policy = "rebuildable denormalization; canonical source identity remains authoritative", launch_profile = "remote", invariant_owner = "cockpit_db::db::remote_attachment_operations table accessors plus 0001_remote_profile.sql" }
"remote_audit_upload_state" = { domain = "remote", rust_owner = "cockpit_db::db::remote_audit_upload table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:remote_attachment", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "remote_attachment", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_db::db::remote_audit_upload table accessors plus 0001_remote_profile.sql" }
"remote_daemon_custody_generation_seq" = { domain = "remote", rust_owner = "cockpit_core::remote_daemon_identity_custody::store table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_core::remote_daemon_identity_custody::store table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_core::remote_daemon_identity_custody::store table accessors plus 0001_remote_profile.sql" }
"remote_daemon_custody_records" = { domain = "remote", rust_owner = "cockpit_core::remote_daemon_identity_custody::store table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:remote_attachment", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "remote_attachment", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_core::remote_daemon_identity_custody::store table accessors plus 0001_remote_profile.sql" }
"remote_principal_audit" = { domain = "remote", rust_owner = "cockpit_db::db::principals table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::principals table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_db::db::principals table accessors plus 0001_remote_profile.sql" }
"remote_rename_artifact_cleanup_intents" = { domain = "remote", rust_owner = "cockpit_db::db::remote_attachment_operations table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:remote_attachment", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "remote_attachment", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_db::db::remote_attachment_operations table accessors plus 0001_remote_profile.sql" }
"remote_rename_journal" = { domain = "remote", rust_owner = "cockpit_db::db::remote_attachment_operations table accessors", status = "remove-from-v0.1", recovery_entrypoint = "cockpit_db::db::remote_attachment_operations table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "not retained by local-v0.1", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_db::db::remote_attachment_operations table accessors plus 0001_remote_profile.sql" }
"retention_meta" = { domain = "retention", rust_owner = "cockpit_db::db::retention table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::retention table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained for the lifetime of the owning schema profile", state_machine = "rust", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::retention table accessors plus 0001_initial.sql" }
"run_invocations" = { domain = "run", rust_owner = "cockpit_db::db::run_invocations + cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::run_invocations + cockpit_db::db::text_artifacts table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::run_invocations + cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"run_invocation_tombstones" = { domain = "run", rust_owner = "cockpit_db::db::run_invocations table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:session", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "session", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::run_invocations table accessors plus 0001_initial.sql" }
"scheduled_jobs" = { domain = "scheduled", rust_owner = "cockpit_db::db::scheduler table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:scheduled_job", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "scheduled_job", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "extended", invariant_owner = "cockpit_db::db::scheduler table accessors plus 0001_extended_profile.sql" }
"sealed_action_grants" = { domain = "sealed", rust_owner = "cockpit_db::db::sealed_actions + cockpit_db::db::sealed_scope table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:sealed", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "sealed", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sealed_actions + cockpit_db::db::sealed_scope table accessors plus 0001_initial.sql" }
"sealed_action_instances" = { domain = "sealed", rust_owner = "cockpit_db::db::sealed_actions table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::sealed_actions table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sealed_actions table accessors plus 0001_initial.sql" }
"sealed_global_project_grants" = { domain = "sealed", rust_owner = "cockpit_db::db::sealed_scope table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:sealed", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "sealed", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sealed_scope table accessors plus 0001_initial.sql" }
"sealed_recovery_audit" = { domain = "sealed", rust_owner = "cockpit_db::db::sealed_actions table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::sealed_actions table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sealed_actions table accessors plus 0001_initial.sql" }
"sealed_state_sagas" = { domain = "sealed", rust_owner = "cockpit_db::db::secure_key table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:sealed", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "sealed", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secure_key table accessors plus 0001_initial.sql" }
"sealed_value_name_tombstones" = { domain = "sealed", rust_owner = "cockpit_db::db::sealed_scope table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:sealed", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "sealed", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::sealed_scope table accessors plus 0001_initial.sql" }
"sealed_value_records" = { domain = "sealed", rust_owner = "cockpit_db::db::sealed_scope + cockpit_db::db::sessions + cockpit_db::db::sealed_values table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:sealed", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "sealed", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sealed_scope + cockpit_db::db::sessions + cockpit_db::db::sealed_values table accessors plus 0001_initial.sql" }
"sealed_values" = { domain = "sealed", rust_owner = "cockpit_db::db::secret_vault + cockpit_db::db::sealed_scope + cockpit_db::db::sessions table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::secret_vault + cockpit_db::db::sealed_scope + cockpit_db::db::sessions table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secret_vault + cockpit_db::db::sealed_scope + cockpit_db::db::sessions table accessors plus 0001_initial.sql" }
"sealed_value_sagas" = { domain = "sealed", rust_owner = "cockpit_db::db::sealed_scope table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:sealed", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "sealed", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sealed_scope table accessors plus 0001_initial.sql" }
"secret_credential_ownership" = { domain = "secret", rust_owner = "cockpit_core::credentials + cockpit_core::secret_ownership + cockpit_core::daemon::server::dispatch table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::credentials + cockpit_core::secret_ownership + cockpit_core::daemon::server::dispatch table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::credentials + cockpit_core::secret_ownership + cockpit_core::daemon::server::dispatch table accessors plus 0001_initial.sql" }
"secret_named_ownership" = { domain = "secret", rust_owner = "cockpit_core::policy + cockpit_core::engine::agent::credentials_rejected_rebuild + cockpit_core::secret_ownership table accessors", status = "launch-required", recovery_entrypoint = "cockpit_core::policy + cockpit_core::engine::agent::credentials_rejected_rebuild + cockpit_core::secret_ownership table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_core::policy + cockpit_core::engine::agent::credentials_rejected_rebuild + cockpit_core::secret_ownership table accessors plus 0001_initial.sql" }
"secret_vault_authority" = { domain = "secret", rust_owner = "cockpit_db::db::secret_vault table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::secret_vault table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retained for the lifetime of the owning schema profile", state_machine = "rust", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secret_vault table accessors plus 0001_initial.sql" }
"secret_vault_inventory_state" = { domain = "secret", rust_owner = "cockpit_db::db::secret_vault table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:secret_vault", retention_policy = "retained for the lifetime of the owning schema profile", state_machine = "rust-and-sql", state_machine_family = "secret_vault", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secret_vault table accessors plus 0001_initial.sql" }
"secret_vault_item_revisions" = { domain = "secret", rust_owner = "cockpit_db::db::secret_vault table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::secret_vault table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secret_vault table accessors plus 0001_initial.sql" }
"secret_vault_items" = { domain = "secret", rust_owner = "cockpit_db::db::secret_vault table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::secret_vault table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secret_vault table accessors plus 0001_initial.sql" }
"secret_vault_keys" = { domain = "secret", rust_owner = "cockpit_db::db::secret_vault table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::secret_vault table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secret_vault table accessors plus 0001_initial.sql" }
"secret_vault_sagas" = { domain = "secret", rust_owner = "cockpit_db::db::secret_vault table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:secret_vault", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "secret_vault", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secret_vault table accessors plus 0001_initial.sql" }
"secure_key_consumer_refs" = { domain = "secure", rust_owner = "cockpit_db::db::secure_key table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::secure_key table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "follows the owning entity and declared foreign-key deletion policy", state_machine = "rust", semantic_class = "junction", identity_policy = "declared composite or stable association identity; relationship scope is part of identity", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secure_key table accessors plus 0001_initial.sql" }
"secure_key_namespaces" = { domain = "secure", rust_owner = "cockpit_db::db::secure_key table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::secure_key table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secure_key table accessors plus 0001_initial.sql" }
"secure_key_sagas" = { domain = "secure", rust_owner = "cockpit_db::db::secure_key table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:secure_key", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "secure_key", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secure_key table accessors plus 0001_initial.sql" }
"secure_key_versions" = { domain = "secure", rust_owner = "cockpit_db::db::secure_key table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:secure_key", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "secure_key", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::secure_key table accessors plus 0001_initial.sql" }
"session_events" = { domain = "session", rust_owner = "cockpit_db::db::needs_attention + cockpit_db::db::verification_ledger + cockpit_db::db::org_sync table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:session", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "session", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::needs_attention + cockpit_db::db::verification_ledger + cockpit_db::db::org_sync table accessors plus 0001_initial.sql" }
"session_fts_docs" = { domain = "session", rust_owner = "cockpit_db::db::session_search table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::session_search table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "rebuildable projection retained with its authoritative source and replaced only by generation-aware rebuild", state_machine = "rust", semantic_class = "projection", identity_policy = "source identity plus projection generation or declared projection key", snapshot_policy = "rebuildable denormalization; canonical source identity remains authoritative", launch_profile = "local", invariant_owner = "cockpit_db::db::session_search table accessors plus 0001_initial.sql" }
"session_goals" = { domain = "session", rust_owner = "cockpit_db::db::session_goals + cockpit_db::db::inference_calls table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::session_goals + cockpit_db::db::inference_calls table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::session_goals + cockpit_db::db::inference_calls table accessors plus 0001_initial.sql" }
"session_plan_docs" = { domain = "session", rust_owner = "cockpit_db::db::session_plan_docs table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::session_plan_docs table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "rebuildable projection retained with its authoritative source and replaced only by generation-aware rebuild", state_machine = "rust", semantic_class = "projection", identity_policy = "source identity plus projection generation or declared projection key", snapshot_policy = "rebuildable denormalization; canonical source identity remains authoritative", launch_profile = "local", invariant_owner = "cockpit_db::db::session_plan_docs table accessors plus 0001_initial.sql" }
"sessions" = { domain = "sessions", rust_owner = "cockpit_db::db::run_invocations + cockpit_db::db::secret_vault + cockpit_db::db::protected_redaction_history table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::run_invocations + cockpit_db::db::secret_vault + cockpit_db::db::protected_redaction_history table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::run_invocations + cockpit_db::db::secret_vault + cockpit_db::db::protected_redaction_history table accessors plus 0001_initial.sql" }
"session_text_artifact_archive_imports" = { domain = "session", rust_owner = "cockpit_db::db::archive_import + cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::archive_import + cockpit_db::db::text_artifacts table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::archive_import + cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"session_text_artifact_event_refs" = { domain = "session", rust_owner = "cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::text_artifacts table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "follows the owning entity and declared foreign-key deletion policy", state_machine = "rust", semantic_class = "junction", identity_policy = "declared composite or stable association identity; relationship scope is part of identity", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"session_text_artifact_projection_pending_sentinel" = { domain = "session", rust_owner = "cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::text_artifacts table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"session_text_artifact_projection_pending_slots" = { domain = "session", rust_owner = "cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::text_artifacts table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"session_text_artifact_quota_reservations" = { domain = "session", rust_owner = "cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:session", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "session", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"session_text_artifact_run_invocation_bindings" = { domain = "session", rust_owner = "cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::text_artifacts table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "follows the owning entity and declared foreign-key deletion policy", state_machine = "rust", semantic_class = "junction", identity_policy = "declared composite or stable association identity; relationship scope is part of identity", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"session_text_artifacts" = { domain = "session", rust_owner = "cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:session", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "session", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"session_user_message_model_envelopes" = { domain = "session", rust_owner = "cockpit_db::db::archive_import + cockpit_db::db::text_artifacts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:session", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "session", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::archive_import + cockpit_db::db::text_artifacts table accessors plus 0001_initial.sql" }
"skill_curator_snapshots" = { domain = "skill", rust_owner = "cockpit_db::db::skill_usage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::skill_usage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "rebuildable projection retained with its authoritative source and replaced only by generation-aware rebuild", state_machine = "rust", semantic_class = "projection", identity_policy = "source identity plus projection generation or declared projection key", snapshot_policy = "rebuildable denormalization; canonical source identity remains authoritative", launch_profile = "local", invariant_owner = "cockpit_db::db::skill_usage table accessors plus 0001_initial.sql" }
"skill_pairs" = { domain = "skill", rust_owner = "cockpit_db::db::skill_pairs table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::skill_pairs table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::skill_pairs table accessors plus 0001_initial.sql" }
"skill_usage" = { domain = "skill", rust_owner = "cockpit_db::db::skill_usage table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::skill_usage table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::skill_usage table accessors plus 0001_initial.sql" }
"subagent_handles" = { domain = "subagent", rust_owner = "cockpit_db::db::subagent_handles table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::subagent_handles table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::subagent_handles table accessors plus 0001_initial.sql" }
"sync_state" = { domain = "sync", rust_owner = "cockpit_db::db::org_sync table accessors", status = "remove-from-v0.1", recovery_entrypoint = "state-machine-family:connector_sync", retention_policy = "not retained by local-v0.1", state_machine = "rust-and-sql", state_machine_family = "connector_sync", semantic_class = "singleton", identity_policy = "schema-enforced singleton or scoped singleton key", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "remote", invariant_owner = "cockpit_db::db::org_sync table accessors plus 0001_remote_profile.sql" }
"tandem_inference" = { domain = "tandem", rust_owner = "cockpit_db::db::archive_import + cockpit_db::db::tandem + cockpit_db::db::session_log table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::archive_import + cockpit_db::db::tandem + cockpit_db::db::session_log table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::archive_import + cockpit_db::db::tandem + cockpit_db::db::session_log table accessors plus 0001_initial.sql" }
"task_artifact_integration_receipts" = { domain = "task", rust_owner = "cockpit_db::db::workspace_lease_artifacts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:task", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "task", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::workspace_lease_artifacts table accessors plus 0001_initial.sql" }
"task_artifacts" = { domain = "task", rust_owner = "cockpit_db::db::workspace_lease_artifacts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:task", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "task", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::workspace_lease_artifacts table accessors plus 0001_initial.sql" }
"task_delegation_children" = { domain = "task", rust_owner = "cockpit_db::db::task_delegations + cockpit_db::db::archive_import + cockpit_db::db::agent_tree_decisions table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::task_delegations + cockpit_db::db::archive_import + cockpit_db::db::agent_tree_decisions table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::task_delegations + cockpit_db::db::archive_import + cockpit_db::db::agent_tree_decisions table accessors plus 0001_initial.sql" }
"task_delegation_jobs" = { domain = "task", rust_owner = "cockpit_db::db::task_delegations + cockpit_db::db::archive_import + cockpit_db::db::agent_tree_decisions table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:task", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "task", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::task_delegations + cockpit_db::db::archive_import + cockpit_db::db::agent_tree_decisions table accessors plus 0001_initial.sql" }
"task_delegation_payloads" = { domain = "task", rust_owner = "cockpit_db::db::sessions + cockpit_db::db::task_delegations + cockpit_db::db::archive_import table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:task", retention_policy = "inline bodies follow row retention; each unique sidecar pathname is removed only through durable cleanup intent", state_machine = "rust-and-sql", state_machine_family = "task", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sessions + cockpit_db::db::task_delegations + cockpit_db::db::archive_import table accessors plus 0001_initial.sql" }
"task_delegation_sidecar_cleanup_intents" = { domain = "task", rust_owner = "cockpit_db::db::sessions + cockpit_db::db::task_delegation_payloads table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:task", retention_policy = "delete only after the named sidecar is durably absent; unresolved intents are retained", state_machine = "rust-and-sql", state_machine_family = "task", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::sessions + cockpit_db::db::task_delegation_payloads table accessors plus 0001_initial.sql" }
"task_delegation_sidecar_prepare_intents" = { domain = "task", rust_owner = "cockpit_db::db::task_delegation_payloads table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:task", retention_policy = "remove after payload commit or after durable orphan cleanup", state_machine = "rust-and-sql", state_machine_family = "task", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::task_delegation_payloads table accessors plus 0001_initial.sql" }
"task_delegation_steers" = { domain = "task", rust_owner = "cockpit_db::db::task_delegations + cockpit_db::db::archive_import table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::task_delegations + cockpit_db::db::archive_import table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::task_delegations + cockpit_db::db::archive_import table accessors plus 0001_initial.sql" }
"task_todo_assignments" = { domain = "task", rust_owner = "cockpit_db::db::task_todos table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::task_todos table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "follows the owning entity and declared foreign-key deletion policy", state_machine = "rust", semantic_class = "junction", identity_policy = "declared composite or stable association identity; relationship scope is part of identity", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::task_todos table accessors plus 0001_initial.sql" }
"task_todo_notes" = { domain = "task", rust_owner = "cockpit_db::db::task_todos table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::task_todos table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::task_todos table accessors plus 0001_initial.sql" }
"task_todos" = { domain = "task", rust_owner = "cockpit_db::db::task_todos table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::task_todos table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::task_todos table accessors plus 0001_initial.sql" }
"tokenizer_calibration" = { domain = "tokenizer", rust_owner = "cockpit_db::db::tokenizer_calibration table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::tokenizer_calibration table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::tokenizer_calibration table accessors plus 0001_initial.sql" }
"tool_call_events" = { domain = "tool", rust_owner = "cockpit_db::db::tool_calls + cockpit_db::db::sessions + cockpit_db::db::stats table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:telemetry", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "telemetry", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::tool_calls + cockpit_db::db::sessions + cockpit_db::db::stats table accessors plus 0001_initial.sql" }
"usage_events" = { domain = "usage", rust_owner = "cockpit_db::db::usage_events table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:telemetry", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "telemetry", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::usage_events table accessors plus 0001_initial.sql" }
"verification_candidate_artifacts" = { domain = "verification", rust_owner = "cockpit_db::db::verification_ledger table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:verification", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "verification", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger table accessors plus 0001_initial.sql" }
"verification_candidates" = { domain = "verification", rust_owner = "cockpit_db::db::verification_ledger table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::verification_ledger table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger table accessors plus 0001_initial.sql" }
"verification_dispatch_attempts" = { domain = "verification", rust_owner = "cockpit_db::db::verification_ledger table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:verification", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "verification", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger table accessors plus 0001_initial.sql" }
"verification_late_results" = { domain = "verification", rust_owner = "cockpit_db::db::verification_ledger table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::verification_ledger table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger table accessors plus 0001_initial.sql" }
"verification_operations" = { domain = "verification", rust_owner = "cockpit_db::db::verification_ledger table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:verification", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "verification", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger table accessors plus 0001_initial.sql" }
"verification_projection_envelopes" = { domain = "verification", rust_owner = "cockpit_db::db::verification_ledger table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:verification", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "verification", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger table accessors plus 0001_initial.sql" }
"verification_projection_events" = { domain = "verification", rust_owner = "cockpit_db::db::verification_ledger table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:verification", retention_policy = "retain for the owning domain audit window; purge only through daemon retention after dependents and holds are resolved", state_machine = "rust-and-sql", state_machine_family = "verification", semantic_class = "immutable-fact", identity_policy = "immutable event or receipt identity; duplicate identity is idempotent and never rewritten", snapshot_policy = "immutable historical attribution; never rewritten to follow current display or configuration state", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger table accessors plus 0001_initial.sql" }
"verification_projections" = { domain = "verification", rust_owner = "cockpit_db::db::verification_ledger table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:verification", retention_policy = "rebuildable projection retained with its authoritative source and replaced only by generation-aware rebuild", state_machine = "rust-and-sql", state_machine_family = "verification", semantic_class = "projection", identity_policy = "source identity plus projection generation or declared projection key", snapshot_policy = "rebuildable denormalization; canonical source identity remains authoritative", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger table accessors plus 0001_initial.sql" }
"verification_syntheses" = { domain = "verification", rust_owner = "cockpit_db::db::verification_ledger table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::verification_ledger table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger table accessors plus 0001_initial.sql" }
"verification_synthesis_artifacts" = { domain = "verification", rust_owner = "cockpit_db::db::verification_ledger table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:verification", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "verification", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::verification_ledger table accessors plus 0001_initial.sql" }
"workspace_leases" = { domain = "workspace", rust_owner = "cockpit_db::db::workspace_lease_artifacts table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:workspace_write", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "workspace_write", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::workspace_lease_artifacts table accessors plus 0001_initial.sql" }
"workspace_trust" = { domain = "workspace", rust_owner = "cockpit_db::db::workspace_trust + cockpit_db::db::sessions table accessors", status = "launch-required", recovery_entrypoint = "cockpit_db::db::workspace_trust + cockpit_db::db::sessions table accessors owns table-specific load/repair; no shared transition-family recovery", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::workspace_trust + cockpit_db::db::sessions table accessors plus 0001_initial.sql" }
"write_scope_leases" = { domain = "write", rust_owner = "cockpit_db::db::workspace_lease_artifacts + cockpit_db::db::write_scope_leases table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:workspace_write", retention_policy = "recover nonterminal rows at daemon boot; retain terminal evidence through the owning domain audit window", state_machine = "rust-and-sql", state_machine_family = "workspace_write", semantic_class = "queue-lease", identity_policy = "stable operation or lease identity with idempotency and generation fencing where mutable", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::workspace_lease_artifacts + cockpit_db::db::write_scope_leases table accessors plus 0001_initial.sql" }
"write_scope_permits" = { domain = "write", rust_owner = "cockpit_db::db::write_scope_leases table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:workspace_write", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "workspace_write", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::write_scope_leases table accessors plus 0001_initial.sql" }
"write_scope_transfers" = { domain = "write", rust_owner = "cockpit_db::db::write_scope_leases table accessors", status = "launch-required", recovery_entrypoint = "state-machine-family:workspace_write", retention_policy = "retain until explicit owner deletion or the documented domain lifecycle completes; cascades follow declared relationships", state_machine = "rust-and-sql", state_machine_family = "workspace_write", semantic_class = "entity", identity_policy = "stable primary or declared natural identity; immutable identifiers are never rewritten", snapshot_policy = "authoritative current state unless the table name or invariant explicitly records historical evidence", launch_profile = "local", invariant_owner = "cockpit_db::db::write_scope_leases table accessors plus 0001_initial.sql" }
